import React from 'react';
import styled from 'styled-components';

const SiderBar = () => {
  return <SiderBarStyle>SiderBar</SiderBarStyle>;
};

export const SiderBarStyle = styled.div``;

export default SiderBar;
